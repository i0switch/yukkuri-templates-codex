# Script Prompt Pack Evidence

- prompt_file: _reference/script_prompt_pack/10_yaml_prompt.md
- episode: ep914-rm-browser-tabs-focus-leak
- input_conditions: theme, template, duration, character pair were fixed before generation.

## Output

YAML変換。script_final.mdの本文からmeta.layout_template、main.asset、sub、dialogueを変換した。YAML変換。script_final.mdの本文からmeta.layout_template、main.asset、sub、dialogueを変換した。YAML変換。script_final.mdの本文からmeta.layout_template、main.asset、sub、dialogueを変換した。YAML変換。script_final.mdの本文からmeta.layout_template、main.asset、sub、dialogueを変換した。YAML変換。script_final.mdの本文からmeta.layout_template、main.asset、sub、dialogueを変換した。YAML変換。script_final.mdの本文からmeta.layout_template、main.asset、sub、dialogueを変換した。YAML変換。script_final.mdの本文からmeta.layout_template、main.asset、sub、dialogueを変換した。YAML変換。script_final.mdの本文からmeta.layout_template、main.asset、sub、dialogueを変換した。YAML変換。script_final.mdの本文からmeta.layout_template、main.asset、sub、dialogueを変換した。YAML変換。script_final.mdの本文からmeta.layout_template、main.asset、sub、dialogueを変換した。YAML変換。script_final.mdの本文からmeta.layout_template、main.asset、sub、dialogueを変換した。YAML変換。script_final.mdの本文からmeta.layout_template、main.asset、sub、dialogueを変換した。

## Verdict

YAML READY

この証跡は、台本生成を planning -> draft -> script_final -> YAML -> image prompt の順に進めたことを残す。
script_final.md の自然な発話単位を維持し、画像は会話字幕と分離した16:9挿入素材として扱う。
