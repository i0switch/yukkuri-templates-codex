# 03_IMAGE_PROMPT_AUDIT

## 目的

直投げ型では、細かい画像プロンプト監査をレンダーの必須ゲートにしない。

このファイルは手動確認用の観点だけを残す。

## 任意確認

- `script_final.md` の対象シーン全文が入っている
- アスペクト比16:9が明記されている
- 会話は別で字幕表示する前提が書かれている
- 画像はコンテンツ部分の挿入画像として指定されている
- LLMが台本全体から作風を判断している
- 会話全文を画像内文字として並べる指示になっていない

## 判定

この監査は任意。
FAILしても `pre-render-gate` や `build-episode` を止めない。
