# Docs Archive

このフォルダは過去ログ、移行記録、監査記録の退避先。通常生成の必読入力ではない。

現行の単一正本は `docs/pipeline_contract.md`、LLM用の最短入口は `docs/agent_fast_path.md`。

このフォルダ内の古い解像度、episode id、テンプレート、成果物名、停止条件は現行仕様として採用しない。必要な場合だけ、履歴確認用として明示的に読む。
